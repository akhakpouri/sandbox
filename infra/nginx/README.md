# nginx (unused)

Early scratch/tutorial manifests — **not applied to the cluster**. Kept for reference only. It also carries the same stock-image port mismatch as the bug already logged in [`docs/project-notes/bugs.md`](../../docs/project-notes/bugs.md) — never worth fixing since nothing depends on this.

If you want an actual nginx deployment in this repo, treat this as a starting point to fix up, not something already working.
