server:
  service:
    type: ClusterIP
configs:
  params:
    server.insecure: true # plain HTTP, skip self-signed. this is local
dex:
  enabled: false # no oidc in this sandbox
notifications:
  enabled: false #no slack/webhook yet.
